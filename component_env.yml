name: tree_modeling
channels:
  - conda-forge
  - anaconda
  - nodefaults
dependencies:
  - python=3.11
  - pip:
    - tree_modeling==X.X.X
    - >-
      --extra-index-url
      https://pkgs.dev.azure.com/${org_name}/rdam/_packaging/${feed_view}/pypi/simple/
